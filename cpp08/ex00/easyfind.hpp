#ifndef EASYFIND_HPP
#define EASYFIND_HPP
#include <string>
#include <iostream>

class	easyfind {

	private:

	public:

		easyfind ( void );
		easyfind ( std::string str );
		easyfind ( const easyfind & var );
		easyfind~ ( void );
};

#endif
